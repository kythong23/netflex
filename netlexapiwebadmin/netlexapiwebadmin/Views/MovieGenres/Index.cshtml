@model IEnumerable<netlexapiwebadmin.Models.MovieGenre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Genre.GenreId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mgenreid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Mgenreid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Mgenreid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
